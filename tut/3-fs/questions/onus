Rust allows the caller to specify the memory layout to provide greater flexibility for the programmer. In C, the
allocator is making a somewhat arbitrary choice that it must remember internally. In Rust, the programmer gets to
specify the layout in case it is relevant to the program.